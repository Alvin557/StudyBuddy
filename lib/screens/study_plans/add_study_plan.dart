import 'package:flutter/material.dart';

class AddStudyPlans extends StatelessWidget {
  const AddStudyPlans({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
